---
description: Moved — see the API section.
hidden: true
icon: plane
---

# Flaunch Create API (moved)

This page has moved to [**API**](../references/api.md), which is the maintained
reference for the web2 API.

The content previously here was an older, unlinked copy of the same
documentation and had drifted out of date — it still described a 60% fair
launch, a `network` field on `/livez`, and Base-only chain support. Rather than
maintain two copies, everything now lives in one place.
